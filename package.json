{
    "dependencies": {
        "jquery": "^3.3.1",
        "jquery-csv": "^1.0.21",
        "moment": "^2.29.3"
    },
    "scripts": {
        "test": "node scripts/check-syntax.js",
        "test:regression": "node scripts/run-tests.js",
        "test:all": "npm test && npm run test:regression",
        "lint": "npm test",
        "check": "npm test",
        "prepare": "node scripts/install-git-hooks.js"
    },
    "devDependencies": {
        "jsdom": "^29.1.1"
    }
}
